/**
 * 
 * @param rate Rate of distance
 * @param distance Distance in meters
 * @returns Number
 */
const calculateDistanceRate = (rate: number, distance: number) => {
    return 0
}

export {
    calculateDistanceRate
}